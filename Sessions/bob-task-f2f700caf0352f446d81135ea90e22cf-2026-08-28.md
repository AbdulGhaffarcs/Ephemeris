# Fix any issues in the following code from Ephemeris/backend/physics/orbit_propagation.py:180-182

Current problems detected:
- [basedpyright] Cannot access attribute "T" for class "MethodType"
  Attribute "T" is unknown ([object Object])

```
    gcrs = satellite.at(times)
    pos_km = gcrs.position.km.T  # (n, 3)

```

Please fix the issues and explain what was wrong.

---

**Status:** active  **Date:** 2026-08-16

---

### 👤 User

Fix any issues in the following code from Ephemeris/backend/physics/orbit_propagation.py:180-182

Current problems detected:
- [basedpyright] Cannot access attribute "T" for class "MethodType"
  Attribute "T" is unknown ([object Object])

```
    gcrs = satellite.at(times)
    pos_km = gcrs.position.km.T  # (n, 3)

```

Please fix the issues and explain what was wrong.

---

### 🤖 Assistant

> 🔧 called: read_file, read_file

Let me read the relevant section of the file first.

> 🔧 **read_file** — Ephemeris/backend/physics/orbit_propagation.py

> 🔧 **read_file** — Ephemeris/backend/physics/orbit_propagation.py

### 🤖 Assistant

> 🔧 called: read_file

Now let me check what type `gcrs.position.km` actually returns from Skyfield — specifically whether it's a method or property:

> 🔧 **read_file** — Ephemeris/backend/physics/orbit_propagation.py

### 🤖 Assistant

> 🔧 called: apply_diff

Now I have enough context. The issue is clear. In Skyfield, `gcrs.position.km` returns a **`(3, n)` ndarray** — 3 rows (x, y, z) and n columns (one per time step). Basedpyright sees `.km` as a `MethodType` (it's typed as a method/callable in Skyfield's stubs), so accessing `.T` on a `MethodType` is flagged as unknown.

The real fix is to call `.km` properly. Looking at Skyfield's API: `gcrs.position.km` **is** a property that returns a numpy array, but Skyfield's type stubs type it as a method. The `.T` transpose is also semantically correct — it converts `(3, n)` → `(n, 3)`.

The fix is to use `np.asarray(gcrs.position.km).T` which:
1. Satisfies the type checker (`.T` is well-known on `np.ndarray`)
2. Is semantically identical at runtime

> 🔧 **apply_diff** — Ephemeris/backend/physics/orbit_propagation.py

### 🤖 Assistant

I also fixed the identical pattern on line 187 (same root cause, same fix).

---

**What was wrong**

Skyfield's type stubs declare `.km` on a `Distance` object with a signature that Basedpyright resolves to `MethodType` rather than `np.ndarray`. Because `MethodType` has no `.T` attribute, the type checker raises:

> Cannot access attribute "T" for class "MethodType"

At runtime `gcrs.position.km` **is** a `(3, n)` numpy array and `.T` works fine — this was purely a static-typing gap between Skyfield's stubs and its actual runtime behaviour.

**The fix**: wrapping with [`np.asarray(...)`](Ephemeris/backend/physics/orbit_propagation.py:181) makes the static type `np.ndarray`, which has a well-known `.T` attribute. The call is a no-op at runtime (the value is already an ndarray), so behaviour is unchanged. The same fix was applied to the parallel `sun_astrometric.position.km.T` expression on line 187.